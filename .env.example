# Скопируйте в `.env` и при необходимости задайте значения.
# Пустой NUXT_PUBLIC_API — прокси /api отключён (см. server/api/[...].ts).

# Базовый URL бэкенда для серверного прокси Nitro (без завершающего слэша)
NUXT_PUBLIC_API=
